name: App header updater

on:
  push:
    paths:
      - '.github/workflows/app-header-update.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check app file
        run: test -f app/index.html && echo ready
